#pragma once
#include <iostream>
#include <fstream>
#include <cmath>
#include <iomanip>
#include <conio.h>

#include <string>
class Matrix
{
public:
	int triangle(int x, double** M, std::string name);
	int quantity(double z, int x, double** M);
};